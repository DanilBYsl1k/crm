export enum Icons {
  user = 'user',
  user_group = 'users-group',
  calendar = 'calendar',
  grid = 'grid',
  user_plus = 'user-plus',
  case = 'case',
  activity = 'activity',
  user_check = 'user-check',
}
